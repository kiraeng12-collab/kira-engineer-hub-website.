{
  "name": "kira-engineer-hub-static",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "node scripts/validate-pricing.js && node scripts/validate-legal-config.js && node scripts/validate-backend.js && echo Static site ready"
  }
}
