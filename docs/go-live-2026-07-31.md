# KIRA VIP — Go-Live Checklist (switch ON 31 July 2026)

A ~30-minute mechanical switch from Stripe **test** to **live**. Do it on **31 July**,
leaving a one-day buffer before 1 August.

> **Never paste a secret key into chat, a commit, or a support ticket.**
> The live key goes into your local `.env` to run one script, then into Vercel. Nowhere else.

Pre-flight (confirmed by owner): Stripe account activated for live payments, business
verified, payout bank account added. ✅

---

## Step 1 — Put the live key in local `.env`

Replace the (now expired) test key in `C:\Users\User\Documents\claude\.env`:

```
STRIPE_SECRET_KEY=sk_live_...        # the LIVE key from the Kira Engineer Hub account
```

Save. This is only so the setup script in Step 2 can talk to live mode.

---

## Step 2 — Create the live products, prices, coupon and webhook

```
cd C:\Users\User\Documents\claude
node scripts/stripe-setup.js --apply --allow-live
```

The script refuses the old DAFFAR account and refuses live keys without `--allow-live`.
It creates (or reuses) in **live mode**: the product, 4 prices (70 / 189 / 50 / 150),
the 20% coupon, and the webhook at `https://www.kiraengineerhub.com/api/stripe/webhook`.

It writes every value — including the **live webhook signing secret** — to
`stripe-setup-output.txt` (gitignored). Open that file; you'll copy from it in Step 3.

---

## Step 3 — Update Vercel (project `kira-engineer`, **Production** scope)

From `stripe-setup-output.txt`, set each of these. **Paste carefully — no leading/trailing spaces.**
(The code now trims these defensively, but don't rely on it.)

| Variable | Value |
|---|---|
| `STRIPE_SECRET_KEY` | `sk_live_...` |
| `STRIPE_WEBHOOK_SECRET` | live `whsec_...` (from the file) |
| `STRIPE_PRICE_KIRA_VIP_MONTHLY` | live price id |
| `STRIPE_PRICE_KIRA_VIP_QUARTERLY` | live price id |
| `STRIPE_PRICE_KIRA_VIP_MONTHLY_FOUNDING` | live price id |
| `STRIPE_PRICE_KIRA_VIP_QUARTERLY_FOUNDING` | live price id |
| `STRIPE_EARLY_BIRD_COUPON_ID` | live coupon id |

Confirm these two are already `true` (they are, from the dry run):

| `CHECKOUT_ENABLED` | `true` |
| `PAYMENT_AUTOMATION_ENABLED` | `true` |

Everything else (Telegram vars, verify secret) is unchanged.

---

## Step 4 — Redeploy

Vercel → Deployments → ⋯ → **Redeploy**. Env changes only take effect on a new build.

---

## Step 5 — Wipe the test-era data

The production DB still holds records from the test-mode dry run (1 entitlement,
1 membership, 40 acceptances, 10 consents, the owner's test customer id). Clear them so
launch starts from zero. The 535 loyalty members and all accounts are kept.

```
node scripts/reset-test-payment-data.js            # preview
node scripts/reset-test-payment-data.js --apply    # execute
```

---

## Step 6 — Verify

```
node scripts/check-launch-setup.js
```

Expect: Stripe account **Kira Engineer Hub (US)**, all 4 **live** prices correct, coupon
present, webhook enabled, both chats invite+remove OK, and the "Deployed environment"
section showing production matches with both switches `true`.

---

## Step 7 — One real end-to-end charge (the only true proof)

1. Buy **one** real membership yourself with a real card.
2. Confirm: entitlement active, bot DMs invites, you can join **group and channel**.
3. In Stripe → cancel + refund that subscription.
4. Confirm you're **removed from both chats** and the entitlement flips.

Then in Stripe, this is a real payout — the refund returns it minus Stripe's fee.

---

## Step 8 — Clean up

- Delete `stripe-setup-output.txt` (holds the live webhook secret).
- Optionally remove the live `STRIPE_SECRET_KEY` line from local `.env` (it lives in Vercel now).

---

## Rollback (if anything looks wrong)

Set `CHECKOUT_ENABLED=false` in Vercel and redeploy. Checkout refuses instantly;
existing members keep access. Diagnose, fix, flip back on.

---

## What must be true before you flip it

- [ ] Stripe account activated for live payments + payout bank added
- [ ] Live products/prices/coupon/webhook created (Step 2)
- [ ] All 7 Stripe vars updated in Vercel Production + redeployed
- [ ] `check-launch-setup.js` clean, account reads **Kira Engineer Hub (US)**, prices are live
- [ ] Test-era data wiped
- [ ] One real charge → access to both chats → refund → removed from both
